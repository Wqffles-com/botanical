/** Env var holding the Postgres URL. The value stays out of git. */
export const DATABASE_URL_ENV = "DATABASE_URL";

/** SQL files live here, relative to this package. Nothing applies them yet. */
export const MIGRATIONS_DIR = "migrations";
