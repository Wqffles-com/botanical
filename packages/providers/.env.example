# Server-side model keys for @botanical/providers.
# The web client never sends or receives these. Do not commit real values.
# Copy into the server environment (shell, Compose, or host secret store).

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
DEEPSEEK_API_KEY=
OPENROUTER_API_KEY=
