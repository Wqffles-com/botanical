import { EmptyState } from "@/components/chat/empty-state";

export default function SettingsPage() {
  return (
    <EmptyState
      title="Settings"
      body="Profiles, tools, MCP, and deployment mode land in the pages UI. The chat composer still requires an explicit profile pick."
    />
  );
}
