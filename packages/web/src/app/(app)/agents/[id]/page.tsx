import { EmptyState } from "@/components/chat/empty-state";

export default function AgentPage() {
  return (
    <EmptyState
      title="Agent"
      body="Agent edit is owned by the identity UI. Chat threads already show this agent's avatar and name."
    />
  );
}
